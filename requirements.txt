fastapi==0.111.0
uvicorn[standard]
httpx==0.27.0
feedparser==6.0.11
apscheduler==3.10.4
python-dotenv==1.0.1
pydantic==2.7.1
python-multipart==0.0.9
aiofiles==23.2.1
gunicorn==23.0.0
# Read the SPIE engine's insights straight from Supabase (same DB the workers
# write to). Without this /patterns can only see the local sqlite demo rows.
asyncpg==0.29.0
# Articles storage on Supabase Postgres. Render's free tier has ephemeral disk,
# so the sqlite file is destroyed on every deploy and the feed resets to zero;
# pgcompat routes the existing sqlite-shaped queries here instead. Sync driver,
# because the ~80 call sites in main.py are synchronous.
psycopg[binary,pool]==3.2.1
# The read cache in cache.py. REDIS_URL / UPSTASH_REDIS_URL is optional — without
# it the cache degrades to its per-process layer and nothing else changes — but
# with more than one worker only the shared layer actually caps database reads.
redis==5.0.7
